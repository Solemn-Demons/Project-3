// Import the database connection module and necessary models

// Import the cleanDB function to remove existing data

// Listen for the 'open' event on the database connection

// Clean the model collections, then seed them with the imported data

// Create new model document
